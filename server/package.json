{
  "name": "rijita-server",
  "version": "1.0.0",
  "description": "RIJITA by Arya Foods - Backend API (Python/FastAPI)",
  "scripts": {
    "dev": "venv/bin/python main.py",
    "build": "venv/bin/pip install -r requirements.txt",
    "start": "venv/bin/python main.py",
    "test": "venv/bin/python -m unittest discover -s tests -t tests -v",
    "seed": "cross-env NODE_OPTIONS=\"--no-deprecation\" tsx src/seeds/seed.ts"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
